# Kneipen-Schlägerei — Python-Abhängigkeiten
# Installiert im lokalen venv (Arbeitsverzeichnis, nicht systemweit!)

# Core
pyotp
qrcode
Pillow
edge-tts
cryptography

# Bitcoin Chain of Trust
hdwallet
bitcoin-utils

# PQ-Crypto (Post-Quantum)
liboqs-python

# KDF-Härtung (Vault-KEK via Argon2id — hard-coded 128MB/3/4/32, kein Fallback)
argon2-cffi>=23.1
